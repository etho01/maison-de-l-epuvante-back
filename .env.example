# Exemple de configuration pour .env.local
# Copiez ce fichier vers .env.local et adaptez les valeurs

###> Base de données ###
# PostgreSQL (Docker)
DATABASE_URL="postgresql://app:!ChangeMe!@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

# PostgreSQL (personnalisé)
# DATABASE_URL="postgresql://user:password@127.0.0.1:5432/maison_de_lepouvante?serverVersion=16&charset=utf8"

# MySQL
# DATABASE_URL="mysql://user:password@127.0.0.1:3306/maison_de_lepouvante?serverVersion=8.0.32&charset=utf8mb4"

# SQLite (développement uniquement)
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"
###< Base de données ###

###> Symfony ###
APP_ENV=dev
APP_SECRET=changez-moi-en-production-avec-une-valeur-aleatoire
###< Symfony ###

###> CORS ###
# Autoriser localhost et 127.0.0.1 sur tous les ports
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'

# En production, limitez aux domaines autorisés
# CORS_ALLOW_ORIGIN='^https?://(www\.)?example\.com$'
###< CORS ###

###> JWT ###
# Les clés sont générées automatiquement dans config/jwt/
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
# La passphrase est générée automatiquement lors de l'installation
JWT_PASSPHRASE=your-jwt-passphrase-here
###< JWT ###

###> Mailer (optionnel) ###
# Pour l'envoi d'emails de vérification et réinitialisation
# MAILER_DSN=smtp://user:pass@smtp.example.com:25

# Gmail (nécessite un mot de passe d'application)
# MAILER_DSN=gmail://username:password@default

# Mailtrap (pour le développement)
# MAILER_DSN=smtp://username:password@smtp.mailtrap.io:2525

# Sendgrid
# MAILER_DSN=sendgrid://KEY@default

# Désactiver l'envoi (stocke les emails dans var/log)
# MAILER_DSN=null://null
###< Mailer ###

###> stripe/stripe-php ###
# Clé de test Stripe (obtenir la vôtre sur https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=sk_test_your_stripe_test_key_here
###< stripe/stripe-php ###